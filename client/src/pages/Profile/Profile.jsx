import { Navigate } from "react-router-dom";
import { useAuth } from "../../context/AuthContext";
export default function Profile(){const {user,ready,logout}=useAuth();if(!ready)return <div className="py-20 text-center text-gray-500">Loading profile…</div>;if(!user)return <Navigate to="/login" replace/>;return <main className="mx-auto max-w-3xl px-6 py-16"><h1 className="text-4xl font-black">Your Profile</h1><section className="mt-8 rounded-2xl border bg-white p-6"><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-full bg-red-100 text-2xl font-black text-red-600">{user.name?.[0]}</span><div><h2 className="text-xl font-bold">{user.name}</h2><p className="text-gray-500">{user.email}</p></div></div><button onClick={logout} className="mt-8 rounded-lg bg-black px-5 py-2.5 text-sm font-semibold text-white">Logout</button></section></main>}
